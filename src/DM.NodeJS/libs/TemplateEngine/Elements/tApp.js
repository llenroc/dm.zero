var app = {};

/* *
 * 解析
 * @elementContent      标签
 * @appInfo       应用
 * @channelInfo   栏目
 * @contentInfo   内容
 * */
app.parse = function(elementContent, appInfo, channelInfo, contentInfo, fileInfo){
    return "test app template engine";
};

module.exports = app;